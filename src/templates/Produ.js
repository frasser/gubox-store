import React from 'react'

import ProductDetail from '../components/ProductDetail'
const Produ = ({pageContext}) => {
    return (
        <ProductDetail {...pageContext} />
    )
}

export default Produ
